# 计划

1. 单测：`getUsingYourToolsSection`、组装顺序；`Bash` 注册与 `runAgentBash`。
2. `agent-bash.ts` + `tool-executor` + `agent-tool-defs` + types。
3. `getUsingYourToolsSection` 接入 `buildAgentSystemPrompt`。
4. `npm test` 相关目录全绿。
