**状态：** 已确认

## 已确认方案

- `getSimpleToneAndStyleSection()`：§9 外部版英文原文（emoji、简短、file_path:line_number、owner/repo#123、工具前不用冒号）。
- `buildAgentSystemPrompt`：`using tools → tone → session guidance → tool rules → project root`。

**验证：** Vitest `UT-agent-system-prompt`（11 用例）。
