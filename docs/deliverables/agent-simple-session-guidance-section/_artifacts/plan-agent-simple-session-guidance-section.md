# 计划 — agent-simple-session-guidance-section

## 阶段

1. 单测：`getSessionSpecificGuidanceSection` 要点、`null`/`interactive`、组装顺序。
2. 实现 `getSessionSpecificGuidanceSection` + `buildAgentSystemPrompt` 插入。
3. `agent-tool-defs.ts` re-export。
4. `npm test tests/unittest/UT-agent-system-prompt` 全绿。
