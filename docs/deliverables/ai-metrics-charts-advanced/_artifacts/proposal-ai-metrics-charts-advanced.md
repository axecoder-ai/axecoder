## 已确认解决方案提案

**状态：** 已确认（用户指定「直接搞进阶版」）

**上下文：**
- **请求：** 仪表盘增加进阶图表：累计 Token、输入分布、SLO、E2E/Token 拆分、来源与成败结构。
- **选定基础：** 提案 2 – 进阶图表包

### 最终方案

- **概述：** 扩展 `ai-metrics-store` 时序与分析字段；`AiMetricsPanel` 增至 9 张图（原 3 + 新 6），底部 Tab 横向滚动，脱离窗 3 列网格。
- **关键变更：** `ai-metrics-store.ts`、`AiMetricsPanel.vue`、`axecoder.d.ts`、i18n、单测
- **验证：** 单测覆盖新聚合；手工有数据后看图
